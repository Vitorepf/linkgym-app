# Copiado automaticamente por `make setup` / `npm run setup`. Não commitar o `.env`.
#
# Simulador iOS / emulador Android no mesmo Mac: localhost funciona.
# Aparelho físico: IP da máquina na LAN, ex. http://192.168.0.10:8080

EXPO_PUBLIC_API_URL=http://localhost:8080
